import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF6D0EB5);        // Purple
  static const Color secondary = Color(0xFF4059F1);      // Blue
  static const Color accent = Color(0xFFFFC107);         // Amber
  static const Color background = Color(0xFFF5F5F5);      // Light grey
  static const Color textPrimary = Color(0xFF212121);    // Dark text
  static const Color textSecondary = Color(0xFF757575);  // Grey text
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color divider = Colors.white54;

  // Add more theme-based or state-based colors as needed
  static const Color error = Colors.redAccent;
  static const Color success = Colors.green;
}
